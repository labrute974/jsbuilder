# Javascript build container image

This repository contains the tools required to be able to build Javascript projects.

## Supported versions

Currently, this only supports `node 10` running on `alpine`.
